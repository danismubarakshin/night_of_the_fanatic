export const initialState = {
	user: {
		name: 'Василий',
		age: 25,
	},
	products: [],
}

export const reducer = (state = initialState, action) => {
	switch (action.type) {
		// cases

		default:
			return state
	}
}
